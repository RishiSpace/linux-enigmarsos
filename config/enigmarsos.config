# EnigmarsOS kernel configuration fragment
# Applied on top of the Arch Linux x86_64 config. Do not use this file alone.
#
# Every option here is documented in config/CONFIG_CHANGES.md.
# Keep this file as small as possible.

# Do not compile for the build machine. ISA floor is x86-64-v3, set in
# PKGBUILD by rewriting arch/x86/Makefile (-march=x86-64-v3). Vanilla
# 7.2 has no CONFIG_X86_64_VERSION.
# CONFIG_X86_NATIVE_CPU is not set

# Primary EnigmarsOS modification: BORE scheduler
CONFIG_SCHED_BORE=y

# BORE base-slice floor (nanoseconds). Matches the patch Kconfig default.
CONFIG_MIN_BASE_SLICE_NS=2000000
